![[Note2-Pic1.png]]I want to redesign version switch . 
"Insert" version selection control in postion **(1)** between "copy iid" and annotations

I want it rendered as light drop down (similiar to version chnage in composer object editor)
- displaying selected version (or latest)
- when opened 
- shows most recent versions (say 10.)
- user can page between versions . add tine data/time from , data/time to selection to minimize number of versions. 
- when user selects version it propogates and used as graph context . 

version selector in explorer no longer needed . this will let to explore and query version as well as inspecting objects