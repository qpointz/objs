Improve Objects view 
![[Pasted image 20260822160441.png]]
**(1)** Make grid using same chrome as in Query (Data / Nodes tab). Keep "add" butoon . Object Id... must be link opening object view as right pane.  Right pane shoud keep current size when object selected . It replaces Matcher and shelf. When Object view closed or no object selected it shows Matcher amd Shelf tabs **(2)** (as now)


**(2)** Must be top aligned with data grid. This will give extra row (same row where statistics currently shown) **(3)** and **(4)** must be moved right side of the "statistics" row

## Improve Shelf and Matcher

![[Pasted image 20260822161220.png]]

- Swap Matcher **(1)** and SHelf **(2)** tabs  
- Shelf must use bold font when it has objects 
- Buttons (3) and (4) must be moved on action row (same row with statiistics)

- 