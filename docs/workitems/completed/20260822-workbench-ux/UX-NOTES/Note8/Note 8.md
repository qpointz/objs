
## Composer  chrome rework

Top element of composer **(1)** must look close to graph-context. Reuse if it possible or create new component. Important is visual identity. 

When I refering to "graph-context" below I am refering to Composer graph.context implementation (when you decide to create separate) context component


![[Pasted image 20260822164227.png]]

**(2)** Must go in position where in graph context "Open" button. in contrast with graph context this button doesn't have menu and opens existing graph only 
**(3)** Goes before Open in graph-context . It has two split "menus" "Blank" creates empty graph. or "Matcher" creates new graph from matcher expression. 